# Tests

Use `make test`, `make test-compat`, and `make test-e2e` to validate the chart locally. The end-to-end flow installs the official External Secrets Operator chart into a disposable kind cluster and then installs this chart on top of it.
