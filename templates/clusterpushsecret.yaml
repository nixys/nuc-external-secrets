{{- include "nuc-external-secrets.renderResources" (dict
  "root" $
  "collection" (.Values.clusterPushSecrets | default dict)
  "kind" "ClusterPushSecret"
  "kindCamel" "clusterPushSecret"
  "clusterScoped" true
) }}