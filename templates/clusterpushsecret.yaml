{{ include "nuc-external-secrets.renderGenericResources" (dict
  "root" $
  "collection" ($.Values.clusterPushSecrets | default dict)
  "kind" "ClusterPushSecret"
  "defaultApiVersion" $.Values.apiVersions.clusterPushSecret
  "namespaced" false
) }}
