{{ include "nuc-external-secrets.renderGenericResources" (dict
  "root" $
  "collection" ($.Values.passwords | default dict)
  "kind" "Password"
  "defaultApiVersion" $.Values.apiVersions.password
  "namespaced" true
) }}
