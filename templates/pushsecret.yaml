{{ include "nuc-external-secrets.renderGenericResources" (dict
  "root" $
  "collection" ($.Values.pushSecrets | default dict)
  "kind" "PushSecret"
  "defaultApiVersion" $.Values.apiVersions.pushSecret
  "namespaced" true
) }}
