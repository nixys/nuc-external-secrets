{{ include "nuc-external-secrets.renderGenericResources" (dict
  "root" $
  "collection" ($.Values.externalSecrets | default dict)
  "kind" "ExternalSecret"
  "defaultApiVersion" $.Values.apiVersions.externalSecret
  "namespaced" true
) }}
