{{- include "nuc-external-secrets.renderResources" (dict
  "root" $
  "collection" (.Values.clusterSecretStores | default dict)
  "kind" "ClusterSecretStore"
  "kindCamel" "clusterSecretStore"
  "clusterScoped" true
) }}