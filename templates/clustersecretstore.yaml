{{ include "nuc-external-secrets.renderGenericResources" (dict
  "root" $
  "collection" ($.Values.clusterSecretStores | default dict)
  "kind" "ClusterSecretStore"
  "defaultApiVersion" $.Values.apiVersions.clusterSecretStore
  "namespaced" false
) }}
