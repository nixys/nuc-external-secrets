{{- include "nuc-external-secrets.renderResources" (dict
  "root" $
  "collection" (.Values.secretStores | default dict)
  "kind" "SecretStore"
  "kindCamel" "secretStore"
  "clusterScoped" false
) }}