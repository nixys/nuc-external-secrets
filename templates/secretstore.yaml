{{ include "nuc-external-secrets.renderGenericResources" (dict
  "root" $
  "collection" ($.Values.secretStores | default dict)
  "kind" "SecretStore"
  "defaultApiVersion" $.Values.apiVersions.secretStore
  "namespaced" true
) }}
