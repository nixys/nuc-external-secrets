{{- range $name := keys ($.Values.mfas | default dict) | sortAlpha }}
{{- $item := get $.Values.mfas $name -}}
{{- if and (ne $name "__helm_docs_example__") (include "nuc-external-secrets.isActiveItem" $item) }}
---
{{ include "nuc-external-secrets.renderNamespacedResource" (dict "root" $ "name" $name "item" $item "kind" "MFA" "apiVersionKey" "mfa") }}
{{- end }}
{{- end }}
