{{ include "nuc-external-secrets.renderGenericResources" (dict
  "root" $
  "collection" ($.Values.mfas | default dict)
  "kind" "MFA"
  "defaultApiVersion" $.Values.apiVersions.mfa
  "namespaced" true
) }}
