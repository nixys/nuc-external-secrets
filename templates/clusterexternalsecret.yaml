{{ include "nuc-external-secrets.renderGenericResources" (dict
  "root" $
  "collection" ($.Values.clusterExternalSecrets | default dict)
  "kind" "ClusterExternalSecret"
  "defaultApiVersion" $.Values.apiVersions.clusterExternalSecret
  "namespaced" false
) }}
