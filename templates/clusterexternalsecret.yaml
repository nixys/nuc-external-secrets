{{- include "nuc-external-secrets.renderResources" (dict
  "root" $
  "collection" (.Values.clusterExternalSecrets | default dict)
  "kind" "ClusterExternalSecret"
  "kindCamel" "clusterExternalSecret"
  "clusterScoped" true
) }}