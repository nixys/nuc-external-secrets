{{ include "nuc-external-secrets.renderGenericResources" (dict
  "root" $
  "collection" ($.Values.githubAccessTokens | default dict)
  "kind" "GithubAccessToken"
  "defaultApiVersion" $.Values.apiVersions.githubAccessToken
  "namespaced" true
) }}
